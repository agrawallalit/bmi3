pipeline {
      stages 
        {
            stage('Build') 
                {
                    step{
                        echo'hello Project is ready to build'
                        }
                }
         }
       }
